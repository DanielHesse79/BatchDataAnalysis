[pytest]
# The archive holds the clinical product's former code. It has moved to its own
# repository and its tests belong there; collecting them here would test code
# this product no longer ships.
norecursedirs = archive dist build .venv .git __pycache__
